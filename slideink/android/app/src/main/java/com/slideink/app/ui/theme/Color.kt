package com.slideink.android.ui.theme

import androidx.compose.ui.graphics.Color

// Cores da marca SlideInk
val SlideInkPrimary = Color(0xFF2563EB)      // Azul principal
val SlideInkPrimaryDark = Color(0xFF1D4ED8)  // Azul escuro
val SlideInkSecondary = Color(0xFF10B981)    // Verde sucesso
val SlideInkAccent = Color(0xFFF59E0B)       // Laranja destaque

// Cores de estado
val ConnectionConnected = Color(0xFF10B981)   // Verde
val ConnectionDisconnected = Color(0xFFEF4444) // Vermelho
val ConnectionConnecting = Color(0xFFF59E0B)   // Amarelo/Laranja

// Cores de ferramenta
val PenColorDefault = Color(0xFF000000)      // Preto
val PenColorRed = Color(0xFFDC2626)          // Vermelho
val PenColorBlue = Color(0xFF2563EB)         // Azul
val PenColorGreen = Color(0xFF10B981)        // Verde
val HighlighterColor = Color(0x60F59E0B)     // Amarelo transparente

// Cores de fundo
val BackgroundDark = Color(0xFF1F2937)
val BackgroundLight = Color(0xFFF9FAFB)
val SurfaceColor = Color(0xFFFFFFFF)

// Cores de texto
val TextPrimary = Color(0xFF111827)
val TextSecondary = Color(0xFF6B7280)
val TextOnPrimary = Color(0xFFFFFFFF)
